# 20260416_01_reactor-python-scope-and-io

## 主題

反応器の最小実装（Python完結）と、将来の HYSYS 連携境界の整理。

## 参照した資料

- `data/report_md/repoet_7.md`
  - 反応式(3.1)〜(3.3)
  - 速度式(3.4)〜(3.6)
  - 速度定数(3.7), (3.8) および表3.1
  - 設計条件としての水蒸気比 5（3.5.2）
  - Appendix の VBA + HYSYS 連携コード
- `data/chem_contest.md`
  - 反応器温度上限の考え方（650℃以下の工業的運転）

## 方針（現時点）

- 反応器は **Python 側で完結**して進める。
- 分離機は **HYSYS 側で設計**する。
- 全体最適化で両者を接続する設計は、HYSYS 未導入の現段階では保留する。

## 過去レポートの読み取り

1. 反応器設計は、速度式・仮定・最適化変数を明示して独立に最適化している。
2. 一方で Appendix には HYSYS と VBA を接続して条件代入・結果読取する記述がある。
3. よって過去レポートは「反応器ロジックの本体が HYSYS 依存」というより、
   「最適化評価の計算エンジンとして HYSYS も使う」運用だったと解釈できる。

## 今回の実装範囲

- 定数定義と反応器計算を分離。
- 反応器の I/O 境界は Protocol で切り、HYSYS 依存コードは持たない。
- `scripts/run_reactor_case.py` は Python 単独で動く最小デモにする。

## 既知の制約

- 妥当性確認の採点基準（手計算・文献・HYSYSのどれを正とするか）は未確定。
- 多段反応器や再加熱器は未実装。
- HYSYS の具体連携方法（CSV連携/COM連携）は未確定。

## 次に決めること

- HYSYS 導入後、最小ケースで接続要否を判断する。
- 全体最適化フェーズでの I/O 形式（CSV or COM）を確定する。
